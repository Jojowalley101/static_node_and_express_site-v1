extends layout 
block content
    article.error 
    h1 #{error.message}
    h2  #{error.status}
    p #{error.stack}
    nav.nav 
        a(href='/')
            span &larr;
            |  Back
            